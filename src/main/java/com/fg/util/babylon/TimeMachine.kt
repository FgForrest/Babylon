package com.fg.util.babylon

class TimeMachine {

    fun determineChangedMsgKeys(primaryMsgBundle: Map<MsgFilePath, Messages>): Map<MsgFilePath, List<MessageKey>> {
        //TODO
        return emptyMap()
    }

}